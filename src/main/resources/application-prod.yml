spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        use_sql_comments: false

logging:
  level:
    root: INFO
    com.cobre.notifier.api: INFO

springdoc:
  swagger-ui:
    enabled: false

